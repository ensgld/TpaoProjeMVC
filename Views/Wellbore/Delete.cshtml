@model Wellbore
<div class="row">
    <div class="col ">
        <div class="card">
            <div class="card-body text-center">
                <p>
                    @Model.wellboreName isimli Wellbore'u silmek istediğinize emin misiniz?
                </p>
                <form asp-controller="Wellbore" asp-action="DeleteConfirm" method="post">
                    <input type="hidden" name="id" value="@Model.WellboreId">
                    <button type="submit" class="btn btn-danger">Sil</button>
                    <a class="btn btn-primary" asp-controller="Wellbore" asp-action="Index">İptal</a>
                </form>

            </div>
        </div>

    </div>

</div>